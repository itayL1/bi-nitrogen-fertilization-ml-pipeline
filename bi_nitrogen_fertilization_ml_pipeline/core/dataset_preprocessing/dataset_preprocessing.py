import pandas as pd


def train_dataset_preprocessing(raw_train_dataset_df: pd.DataFrame) -> pd.DataFrame:
    raise NotImplementedError


def inference_dataset_preprocessing(raw_inference_dataset_df: pd.DataFrame) -> pd.DataFrame:
    raise NotImplementedError
